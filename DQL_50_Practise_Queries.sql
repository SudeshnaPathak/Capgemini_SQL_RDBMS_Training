SELECT ENAME , SAL FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME = "ADAMS");
SELECT ENAME , SAL FROM EMP WHERE SAL < (SELECT SAL FROM EMP WHERE ENAME = "KING");
SELECT ENAME , DEPTNO FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME = "JONES");
SELECT ENAME , JOB FROM EMP WHERE JOB IN (SELECT JOB FROM EMP WHERE ENAME = "JAMES");
SELECT EMPNO , ENAME , SAL*12 ANNUAL_SAL FROM EMP WHERE SAL*12 > (SELECT SAL*12 FROM EMP WHERE ENAME = "WARD");
SELECT ENAME , HIREDATE FROM EMP WHERE HIREDATE < (SELECT HIREDATE FROM EMP WHERE ENAME = "SCOTT");
SELECT ENAME , HIREDATE FROM EMP WHERE HIREDATE > (SELECT HIREDATE FROM EMP WHERE JOB = "PRESIDENT");
SELECT ENAME , SAL FROM EMP WHERE SAL < (SELECT SAL FROM EMP WHERE ENAME = "KING");
SELECT ENAME , SAL FROM EMP WHERE SAL < (SELECT SAL FROM EMP WHERE EMPNO = 7839);
SELECT * FROM EMP WHERE HIREDATE < (SELECT HIREDATE FROM EMP WHERE ENAME = "MILLER");
SELECT ENAME , EMPNO , SAL FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME = "ALLEN");
SELECT ENAME , SAL FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME = "MILLER") AND SAL < (SELECT SAL FROM EMP WHERE ENAME = "ALLEN");
SELECT * FROM EMP WHERE DEPTNO = 20 AND JOB IN (SELECT JOB FROM EMP WHERE ENAME = "SMITH");
SELECT * FROM EMP WHERE JOB = "MANAGER" AND DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME = "TURNER");
SELECT ENAME , HIREDATE FROM EMP WHERE HIREDATE >= "1981-01-01" AND HIREDATE < (SELECT HIREDATE FROM EMP WHERE ENAME = "KING");
SELECT ENAME , SAL , SAL*12 ANNUAL_SAL FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME ="BLAKE") AND SAL < 3500;
SELECT * FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME ="SCOTT") AND SAL < (SELECT SAL FROM EMP WHERE ENAME = "KING");
SELECT ENAME FROM EMP WHERE ENAME LIKE "A%" AND DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME = "BLAKE");
SELECT ENAME , COMM FROM EMP WHERE COMM IS NOT NULL AND JOB = (SELECT JOB FROM EMP WHERE ENAME = "SMITH");
SELECT * FROM EMP WHERE JOB = "CLERK" AND DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME = "TURNER");
SELECT ENAME , SAL , JOB FROM EMP WHERE SAL*12 > (SELECT SAL*12 FROM EMP WHERE ENAME = "SMITH") AND SAL < (SELECT SAL FROM EMP WHERE ENAME = "KING");
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME = "SMITH");
SELECT DNAME , LOC FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME = "KING");
SELECT LOC FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE EMPNO = 7902);
SELECT * FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME LIKE "%R");
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE JOB = "PRESIDENT");
SELECT ENAME , DEPTNO FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME = "ACCOUNTING");
SELECT ENAME , SAL , DEPTNO FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE LOC = "CHICAGO");
SELECT * FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME = "SALES");
SELECT * , SAL*12 ANNUAL_SAL FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE LOC = "NEW YORK");
SELECT ENAME FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME = "OPERATIONS");
SELECT ENAME FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME = "SCOTT") AND DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME = "ACCOUNTING");
SELECT * FROM EMP WHERE JOB = "MANAGER" AND DEPTNO = (SELECT DEPTNO FROM DEPT WHERE LOC = "CHICAGO"); 
SELECT ENAME , SAL FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME = "KING") AND DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME = "ACCOUNTING");
SELECT * FROM EMP WHERE JOB = "SALESMAN" AND DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME = "SALES");
SELECT ENAME , SAL , JOB , HIREDATE FROM EMP WHERE HIREDATE < (SELECT HIREDATE FROM EMP WHERE ENAME = "KING") AND DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME = "OPERATIONS"); 
SELECT * FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME LIKE "%S");
SELECT COUNT(ENAME) NAME, DNAME FROM EMP NATURAL JOIN DEPT WHERE ENAME LIKE "%A%" GROUP BY DNAME;
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME LIKE "%A%");
SELECT DNAME , LOC FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE SAL = 800);
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE COMM IS NOT NULL);
SELECT LOC FROM EMP NATURAL JOIN DEPT WHERE COMM IS NOT NULL AND DEPTNO = 40;

-- MAX & MIN
SELECT ENAME FROM EMP WHERE SAL IN (SELECT MAX(SAL) FROM EMP);
SELECT ENAME FROM EMP WHERE SAL IN (SELECT MIN(SAL) FROM EMP);
SELECT ENAME , HIREDATE FROM EMP WHERE HIREDATE IN (SELECT MIN(HIREDATE) FROM EMP);
SELECT ENAME , HIREDATE FROM EMP WHERE HIREDATE IN (SELECT MAX(HIREDATE) FROM EMP);
SELECT ENAME , COMM FROM EMP WHERE COMM IN (SELECT MIN(COMM) FROM EMP);
SELECT ENAME , COMM , SAL FROM EMP WHERE COMM IN (SELECT MAX(COMM) FROM EMP);
SELECT * FROM EMP WHERE EMPNO IN (SELECT MAX(EMPNO) FROM EMP);
SELECT * FROM EMP WHERE HIREDATE IN (SELECT MIN(HIREDATE) FROM EMP);
SELECT * , SAL*12 ANNUAL_SAL FROM EMP WHERE SAL IN (SELECT MIN(SAL) FROM EMP);
SELECT ENAME , SAL*12 ANNUAL_SAL FROM EMP WHERE SAL > (SELECT MAX(SAL) FROM EMP WHERE JOB = "SALESMAN");

